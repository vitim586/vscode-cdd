#include <stdio.h> // acrescentamos o "d" da palavra include e o escaço

int main() { // acrescentamos o espaço entre "()" e "{"
   int n1, n2, soma;
   printf("Digite dois numeros: ");
   scanf("%d %d", &n1, &n2);
   soma =(n1 + n2);  // adicionamos "()" 
   printf("A soma dos numeros é %d\n", soma);

return 0;
}
